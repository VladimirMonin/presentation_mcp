# title_youtube — Титульный слайд YouTube

> 📐 **Макет:** 1 изображение (логотип квадратной формы)  
> 🎯 **Рекомендуемые пропорции:** 1:1 (квадрат)  
> 🎬 **Назначение:** Титульные слайды для YouTube роликов

## Описание

Специальный макет для создания титульных (заставочных) слайдов YouTube видео. Размещает квадратный логотип или аватар в центре слайда вместе с текстовыми полями:

- **Заголовок** (`title`) — главное название ролика
- **Подзаголовок** (`subtitle`) — дополнительное описание или тема
- **Номер серии** (`series_number`) — для видео-серий (необязательно)

Идеально подходит для:

- Заставок YouTube роликов
- Титульных слайдов подкастов
- Обложек видео-курсов
- Интро презентаций с брендированием

## Технические характеристики

- **Требуемое количество изображений:** 1
- **Тип изображения:** квадратный логотип или аватар
- **Координаты:** left=14.8 см, top=3.4 см
- **Максимальные размеры:** 5.0 см × 5.0 см
- **Пропорции области:** 1:1 (квадрат)

## Поля конфигурации

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `layout_type` | string | ✅ Да | Должно быть `"title_youtube"` |
| `title` | string | ✅ Да | Заголовок (название ролика) |
| `subtitle` | string | ❌ Нет | Подзаголовок или описание |
| `series_number` | string | ❌ Нет | Номер серии (например: "#42", "Ep.5") |
| `images` | array | ✅ Да | Массив с **1 квадратным изображением** (логотип) |

## Пример использования

### JSON конфигурация

```json
{
  "slide_type": "youtube_title",
  "layout_type": "title_youtube",
  "title": "Создаём презентации автоматически",
  "subtitle": "Python + PowerPoint = Магия!",
  "series_number": "#7",
  "images": [
    "images/channel_logo.png"
  ]
}
```

### JSON конфигурация (без номера серии)

```json
{
  "slide_type": "youtube_title",
  "layout_type": "title_youtube",
  "title": "Полный гайд по Python MCP",
  "subtitle": "Model Context Protocol для начинающих",
  "images": [
    "images/python_logo.png"
  ]
}
```

### Результат

```
┌─────────────────────────────────────────┐
│                                         │
│   Создаём презентации автоматически     │ ← title
│   Python + PowerPoint = Магия!          │ ← subtitle
│                 #7                      │ ← series_number
│                                         │
│             ┌───────┐                   │
│             │ LOGO  │                   │ ← images[0] (квадрат)
│             └───────┘                   │
│                                         │
└─────────────────────────────────────────┘
```

## Советы по использованию

✅ **Рекомендуется:**

- Использовать квадратные изображения (1:1): 500×500px, 1000×1000px, 2000×2000px
- Формат: PNG с прозрачным фоном (для логотипов)
- Высокое разрешение: минимум 1000×1000 пикселей
- Чёткий контрастный логотип с минимальными деталями
- Заголовок: краткий и ёмкий (до 60 символов)
- Подзаголовок: уточняющая информация (до 80 символов)

❌ **Не рекомендуется:**

- Широкие/вертикальные изображения (будут обрезаны до квадрата)
- Слишком длинные заголовки (перенос на несколько строк ухудшает читаемость)
- Логотипы низкого разрешения (будут размытыми)
- Использовать для обычных контентных слайдов (это только для титров!)

## Связанные макеты

- Для контентных слайдов после титра → `single_wide`, `single_tall`, `two_stack`
- Для обложки с горизонтальным изображением → `single_wide`

## Примеры реальных конфигураций

### Полный пример в `doc/samples/youtube_title_example.json`

```json
{
  "template_path": "templates/youtube_base.pptx",
  "layout_name": "YouTubeTitleLayout",
  "output_path": "youtube_intro.pptx",
  "slides": [
    {
      "slide_type": "youtube_title",
      "layout_type": "title_youtube",
      "title": "🐍 Python MCP для AI-агентов",
      "subtitle": "Подключаем LLM к реальным инструментам",
      "series_number": "#12",
      "images": ["images/mcp_logo.png"]
    }
  ]
}
```

### Использование с audio (title + контент)

См. полный пример в `doc/samples/audio_example.json` — там показано как комбинировать `title_youtube` с контентными слайдами и аудио.

---

**📝 Примечание:** Этот макет зарегистрирован в `config/settings.py` как `YouTubeTitleSlideConfig` и является частью модели `models/slide_types.py`.
