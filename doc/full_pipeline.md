# Полный Пайплайн Создания Курса с Использованием ИИ агентов и скриптов

Этот документ описывает полный процесс создания учебного курса с использованием комбинации ИИ агентов, скриптов и человеческого участия. Мы разобьем процесс на два основных этапа: "Проектирование" и "Производство". Каждый этап будет подробно описан с указанием ролей, действий и ожидаемых результатов

## Инструменты

1. Скрипты:
    - Скрипт который конвертирует MD документы в HTML в красивом оформлении. Использвется для создания лонгридов. Требует особого форматирования MD файлов. Собирает в т.ч. медиа, картинки, аудио, видео, и подставляет их в HTML, копируя в папку с результатом. На выходе получается готовый для публикации HTML файл с медиа.
    - Скрипт который умеет принемать на вход JSON, и использует шаблон PowerPoint (PPTX) как "API" для создания презентации. Подставляет следующие вещи:
        - Картинки (Одну горизонтальную, Одну вертикальную, две картинки на слайд - там есть несколько режимов)
        - Номер слайда
        - Название слайда
        - Текст для начитки (Speaker Notes)
        - В будущем хочу добавить возможность подкреплять аудио (готовая AI озвучка)
    - Скрипт создания "лонгридов" - берет на вход план MD файл, с разделителями `******`, берет дополнительный контекст. Его задача собрать большой лонгрид. Это может быть один урок из 5-10 генераций, или учебник по Django шаблонам из 20-40 генераций. отправляет в ИИ модель для генерации:
        - Весь план
        - Весь контекст
        - Часть плана (с указанием ты генерируешь это)
        - Умеет обрабатывать вставленные картинки (они предварительно обрабатываются и прикладываются так же каждый запрос)

2. Инструменты для AI агентов:
    - Context7 - Большой сторонний RAG со свежайшей документацией всех крупных преоктов (используется агентом для сбора информации)
    - Brave Search - Поисковый инструмент - используется агентом для сбора информации в сети
    - Gemini media инструмент - умеет анализировать изображение (для моделей без визуального ввода), генерировать изображения, а так же анализировать аудио (в планах сделать генерацию аудио) - Все это проходит через Gemini models 2.5 серии (от lite и flash до pro)

Дальше будет универсальный план для "больших курсов". Однако он годен и может быть применен и для небольших курсов, серий видео или просто сбора качественных учебных материалов, которые как могут быть форматированны в презентации и сняты в видео, так и нет
---

## 🏛️ Этап 1: Проектирование ("Большой План")

Этот этап выполняется **один раз** для всего курса. Его цель — создать "дорожную карту" (`master_plan.md`).

- **Шаг 1.1: Инициация (Человек)**
  - **Действие:** Вы определяете тему ("Введение в Docker") и "объем" ("не сильно много"). На входе уже может быть какая-то задумка, в виде черновиков, заметок, ссылок на статьи.
  - **Результат:** Исходный "заказ". Понимание того, что вы хотите изучить.

- **Шаг 1.2: Разведка (Агент-Исследователь)**
  - **Действие:** Вы даете команду "дешевой" модели (или вашему поисковому агенту): "Проведи исследование по 'Docker'. Найди ключевые концепции, частые вопросы новичков".
  - **Результат:** "Карта местности" (список концепций: Image, Container, Dockerfile, Volumes и т.д.). Один или несколько документов с собранной информацией.

- **Шаг 1.3: Стратегическая Сессия (Агент-Стратег)**
  - **Действие:** Вы даете "карту местности" "дорогой" модели: "На основе этих концепций, предложи 3 'сквозных проекта' для их изучения".
  - **Результат:** 3-5 вариантов "сюжета" (как мы делали: "Сайт-визитка", "Счетчик Flask+Redis" и т.д.).

- **Шаг 1.4: Утверждение (Человек)**
  - **Действие:** Вы выбираете: "ОК, делаем 'Счетчик Flask+Redis'".
  - **Результат:** Финальное "ТЗ" для ИИ.

- **Шаг 1.5: Создание "Мастер-Плана" (Агент-Стратег)**
  - **Действие:** Вы даете команду: "Сгенерируй детальный "мастер-план" для проекта 'Счетчик Flask+Redis'. Разбей на модули и уроки. Для каждого урока укажи:
        1. `title` (Название)
        2. `type` (Тип: 'text', 'guide', 'project')
        3. `learning_objectives` (Цели обучения)
        4. `research_needed` (Список тем для 'Сбора', если ты не владеешь ими, или всеми нюансами)
  - **Результат:** Файл `master_plan.md`. Это ваш "бэклог" на весь курс.

Ожидаемый форамат `master_plan.md`:

Для курсов состоящих из модулей и уроков, разбивка на модули и уроки.
Для серии видео, без модулей, просто список уроков. Может быть это вообще будет атомарный материал, тогда просто план одного урока (видео-статьи-проект).

Этот пример для курса HTMX через TODO-приложение (Мы уже решили что сквозной пример — TODO-приложение, а так же агенты собрали все основные возможности HTMX):

```md
# 📋 Детальный план изучения HTMX через TODO-приложение

## 🎯 Цель: Последовательное освоение всех возможностей HTMX

---

## **Модуль 1: Основы HTMX (Базовый CRUD)**

### Урок 1.1: Простые GET/POST запросы

- **Задачи:**
  - Загрузка списка задач при открытии страницы (`hx-get`, `hx-trigger="load"`)
  - Добавление новой задачи через форму (`hx-post`)
  - Отображение пустого состояния "Задач пока нет"

- **Изучаем:**
  - `hx-get`, `hx-post`
  - `hx-target` (куда вставлять)
  - `hx-swap="innerHTML"` (по умолчанию)

### Урок 1.2: DELETE и стратегии замены

- **Задачи:**
  - Удаление задачи с полным удалением элемента (`hx-delete`, `hx-swap="outerHTML"`)
  - Использование `closest` для поиска родителя

- **Изучаем:**
  - `hx-delete`
  - `hx-swap="outerHTML"` vs `innerHTML`
  - Селектор `closest .classname`

...
---

## **Модуль 7: Управление данными**

### Урок 7.1: Включение данных

- **Задачи:**
  - Отправка данных из других форм (`hx-include`)
  - Фильтрация параметров (`hx-params`)
  - Добавление статических значений (`hx-vals`)

- **Изучаем:**
  - `hx-include="[name='filter']"`
  - `hx-params="*"` или `"not param1"`
  - `hx-vals='{"priority": "high"}'`

### Урок 7.2: Динамические значения

- **Задачи:**
  - JavaScript вычисляемые значения (`hx-vals="js:...")`)
  - Добавление временных меток, UUID
  - Передача состояния UI

- **Изучаем:**
  - `hx-vals="js:{timestamp: Date.now()}"`
  - Выполнение JS для формирования данных

---

## **Модуль 8: HTTP заголовки**
```

******

## 🏭 Этап 2: Производство (Цикл для *Одного* Урока)

Теперь мы запускаем "конвейер". Цель детализировать каждый урок и получить детальный md с файл урока с YAML фронтматтером в вверху. Так можно будет понять, какие ресуры есть, какие нужно собрать, какие были собраны уже ранее, и переиспользовать этот контент в будущем. MD + YAML выбран как формат, который удобно читать челвоеку и агенту.

Это не статичный документ, а "живой" файл, который постепенно наполняется по мере прохождения этапов.

Этот цикл повторяется для *каждой* записи в `master_plan.md`. Возьмем для примера `lesson_03.yaml` (тип 'project').

====== НАЧАЛО ДОКУМЕНТА

```yaml
project: "Курс по HTMX"
lesson: 3
lesson_dir: "doc/lesson_3.md"
title: "Базовые GET запросы в HTMX"
objective: "Научиться использовать HTMX для выполнения базовых GET запросов и обновления частей страницы без перезагрузки страниц."
outcome: "После прохождения урока, студент сможет создавать веб-страницы, которые динамически загружают контент с сервера с помощью HTMX GET запросов."
research_needed: true
research_topics:
  - "Основные проблемы и ошибки студентов при GET запросах в HTMX (Brave Поиск)"
  - "GET запросы в HTMX (Context7 поиск)"
research_links:
  - false
code_samples: false
long_read: false
presentation_dir: "presentations/lesson_3"
presentation_ready: false
video_dir: "videos/lesson_3"
video_ready: false
hash_commit: false
context:
  - "doc/lessons/lesson_01/researches_01/research_1.md"
  - "doc/lessons/lesson_01/code_samples_01/sample_1.py"
```

```md
# Описание ключей

- `project`: Название проекта или курса.
- `lesson`: Номер урока.
- `lesson_dir`: Путь к файлу с материалами урока.
- `title`: Заголовок урока.
- `objective`: Цель урока.
- `outcome`: Ожидаемый результат после прохождения урока.
- `research_needed`: Флаг, указывающий, требуется ли исследование для урока
- `research_topics`: Список тем для исследования.
- `research_links`: Список файлов с готовыми исследованиями (наполняется после проведения исследования).
- `code_samples`: Флаг, указывающий, есть ли примеры кода для урока. (Может быть строкой или списком путей к файлам с примерами кода).
- `long_read`: Флаг, указывающий, готов ли лонгрид для урока.
- `presentation_dir`: Путь к директории с презентацией для урока.
- `context`: Список путей к файлам, которые ПОЙДУТ В ФОНОВЫЙ контекст при работе скрипта-генератора лонг-ридов. Это НЕ обязательно будут все материалы которые есть, мы решаем в процессе, что именно нужно добавить в контекст.
- `hash_commit`: Хэш коммита, связанный с уроком. Может отсутствовать. Будет, в сложных проектах, где уроки связаны с кодовой базой. (строка или список)

# Что может содержать этот документ?

Является единым источником правды по уроку.
Хранит все артефакты. Мысли, идеи, исследования, код, презентации, видео и т.п.

Вероятно не должен содержать рыбу урока (потому что для презентации, лонгрида, подкаста это будет разные рыбы)


- Ссылки на файлы кодовой базы (для 'project' типа)
- Ссылки на примеры кода (для 'guide' типа или если мы решили дублировать код из кодовой базы в doc для наглядности)
- Цитаты и описание в каком конетксте это можно использовать - на исследования
- Вставки (MD ссылки изображений) - фото людей, мемы, скриншоты интерфейсов и т.п.

Вроде живой вечнозеленой заметки. Точка старта для меня, и всех агентов.
А дальше, агенты могут готовить планы, рыбы для скриптов, кодовую базу и т.п.

ПРИМЕР СОДЕРЖИМОГО ЭТОГО ФАЙЛА:

# Урок 1.0: Знакомство с HTMX

## Что такое HTMX?
[Текст. Без кода]
- Что такое HTMX
- Кто и когда придумал HTMX
{Использовать данные из research_1.md
Детально рассказать про автора, кто он, чем известен, какие задачи решал
Его высказывания про HTMX из интервью
- Фото автора
}

******

## Как синтаксически выглядят HTMX атрибуты?
- Идея простая - все типовые действия делаются через HTML атрибуты
- Любой тег может быть "расширен" HTMX атрибутами
******
### Простой пример
- Пример простого GET запроса (тут кодовый сэмпл прямо в плане)
```html
...
```

- Объяснение кода

====== КОНЕЦ ДОКУМЕНТА

Структура файлов и папок проекта:

```

- src/ # Например Django проект
- doc/
  - prompts/ # Общие промпты для агентов
  - master_plan.md
  - lessons/
    - lesson_01/
      - lesson_01.md
      - code_samples_01/
        - sample_1.py
      - researches_01/ # Результаты исследований для урока 1
        - research_1.md
      - presentations_01/ # Подготовка слайдов для урока 1
        - slides_config.json # JSON конфиг для скрипта PPTX
        - slide_1_01.md
        - slide_2_01.md
        - lesson_01.pptx
      - assets_01/ # Скриншоты, мемы, фото людей
        - image1.png
        - code_sample_1_-_01.png
    - lesson_02/
    - lesson_03/

```

### ✒️ Шаг 2.1: Инициация "Источника Правды" (Диспетчер)

- **Подшаг 2.1.1:** Диспетчер читает `master_plan.md`, находит следующий урок (Урок 3).
- **Подшаг 2.1.2:** Диспетчер создает директорию `doc/lessons/lesson_03/`.
- **Подшаг 2.1.3:** Диспетчер создает файл `doc/lessons/lesson_03/lesson_03.md`.
- **Подшаг 2.1.4:** Диспетчер **заполняет Frontmatter (YAML шапку)**, копируя данные из Мастер-Плана: `title`, `objective`, `outcome`.
- **Подшаг 2.1.5:** (Важно) Диспетчер прописывает дефолтные пути для будущих ассетов в ключи YAML (даже если файлов там еще нет), чтобы агентам было куда их класть.
  - `lesson_dir: "doc/lessons/lesson_03/lesson_03.md"`
  - `research_needed: true` (если указано в плане)

**Результат:** Файл `lesson_03.md` создан. В нем есть мета-данные, но **Тело документа (после второго `---`) пустое**.

### 🏗️ Шаг 2.2: Сбор и Ассеты (Агенты-Коллекторы)

В этом шаге мы собираем **всё, что не является текстом урока**. Если это урок по теории, "диаграмма" может быть схемой HTTP запроса. Если это урок про личность — фото автора. Если это гайд по интерфейсу — скриншоты. *Агент решает, что искать, исходя из `objective` урока или явных указаний в промпте.*

- **Подшаг 2.2.1 (Исследование):** Если `research_needed: true`, запускается Агент-Исследователь.
  - Он ищет инфо по темам из `research_topics`.
  - Он сохраняет результаты в `doc/lessons/lesson_03/researches/research_1.md`.
  - **Действие:** Скрипт обновляет поле `context` в `lesson_03.md`, добавляя туда путь к `research_1.md`.
  - Это может быть чат-бот исследователь (доступен Gemini по подписке), это может быть твой собственный RAG агент с Context7 и Brave Search.

- **Подшаг 2.2.2 (Визуальные Ассеты - Предварительные):** Запускается инструмент media (Gemini).
  - Он поможет составить "карту" скринштов (вроде каталога - где есть файл с описанием всех скринов которые я сделал)
  - Он так же может помочь расшифровать аудио заметки, котоырые я делал, или сгенерировать графику для урока (в т.ч. по имеющимся ассетам - в том же стиле или этого же персонажа)
  - Сохраняет файл в `doc/lessons/lesson_03/assets/author_photo.jpg`.
  - **Действие:** Мы пока не знаем, куда именно в текст встанет картинка, поэтому агент просто кладет их в отдельный каталог, указывая название, относительный путь и что изображено на изображении

### 🧑‍💻 Шаг 2.3: Код (Человек-Эксперт + Скрипт)

*Этот шаг нужен только для уроков типа `project`.* Там, где предполагается что нам нужна большая кодовая база (вроде ООП или Django проектов)

- **Подшаг 2.3.1:** Ты получаешь уведомление. Идешь в `src/`, пишешь код, делаешь коммит.
- **Подшаг 2.3.2:** Ты (или скрипт) вставляешь хэш коммита в поле `hash_commit` в `lesson_03.md`.
- **Подшаг 2.3.3 (Авто-скриншоты):** Ты запускаешь свой тул "код-в-картинку".
  - Он берет измененные файлы из коммита.
  - Генерирует красивые PNG с подсветкой синтаксиса.
  - Кладет их в `doc/lessons/lesson_03/assets/code_view.png`.
  - **Действие:** Добавляет пути к этим картинкам каталоге изображений.
  - Нейминг должен быть идентичный. Если примеры кода подписаны вроде `sample_1.py`, то и картинка должна быть `sample_1.png`.

### 🧑‍🍳 Шаг 2.4: Синтез "Рыбы" (Агент-Писатель)

*Самый важный шаг. Превращение мета-данных в контент.*

- **Подшаг 2.4.1:** Скрипт собирает контекст. Он читает `lesson_03.md`.
  - Берет `objective`, `outcome`.
  - Вычитывает файлы, указанные в `context` (исследования, примеры кода).
  - Видит список `available_assets` (какие картинки есть в наличии).
- **Подшаг 2.4.2:** Отправляет запрос в Модель (Writer Persona).
  - *Промпт:* "Напиши подробный урок в формате Markdown. Используй структуру заголовков. Вставь ссылки на картинки из списка `available_assets` там, где это логично по смыслу (используй синтаксис `![alt](path)`). Опирайся на факты из исследования."
- **Подшаг 2.4.3:** Модель генерирует Markdown текст (Тело урока).
- **Подшаг 2.4.4:** Скрипт дописывает этот текст в `lesson_03.md` после Frontmatter-а.

**Результат:** Теперь `lesson_03.md` — это полноценный читаемый документ с картинками и кодом. Единый источник правды готов.

### 🛠️ Шаг 2.5: Компиляция (Роботы-Сборщики)

*Здесь мы уважаем разницу форматов твоих инструментов.*

#### Ветка А: Генерация Лонгрида (HTML)

- **Подшаг 2.5.A.1:** Запускается твой скрипт `MD_to_HTML`.

- **Подшаг 2.5.A.2:** Он берет `lesson_03.md`. Ему **не нужно** ничего генерировать. Он просто парсит Markdown.
- **Подшаг 2.5.A.3:** Он видит `![image](assets/code.png)`, подтягивает картинку, конвертирует MD в HTML.
- **Результат:** Красивая статья `lesson_03.html`.

#### Ветка Б: Генерация Презентации (Атомарный подход)

*Цель: Превратить линейный Markdown-урок (`lesson_03.md`) в набор файлов слайдов и сборочный JSON.*

- **Подшаг 2.5.B.1: Сценарный План (Агент-Сценарист)**

  - **Вход:** `lesson_03.md` (полный текст урока).
  - **Действие:** Агент анализирует текст и разбивает его на логические блоки (слайды).
  - **Результат:** Он создает файлы `doc/lessons/lesson_03/presentations_03/slide_01.md`, `slide_02.md` и т.д.
  - *Содержимое файла слайда:* Только текст для начитки (Speaker Notes). Никакого JSON, чистый текст, чтобы модели было легко писать большие объемы.

- **Подшаг 2.5.B.2: Визуальная Режиссура (Gemini Media + Агент)**

  - **Действие:** Агент проходит по каждому созданному слайду и решает, какая картинка нужна.
  - *Вариант 1 (Есть в ассетах):* Если в `lesson_03.md` рядом с этим текстом была ссылка на скриншот кода (`code_view.png`) или диаграмму — он берет путь к ней.
  - *Вариант 2 (Нужна генерация):* Если слайд требует визуализации (метафора, абстракция, обложка), а картинки нет — Агент формирует промпт для **Gemini Media**.
  - **Генерация:** Gemini генерирует изображение, сохраняет его в `assets_03/generated_slide_02.png`.
  - Конечная цель, чтобы в каждый слайд были встроены изображения

- **Подшаг 2.5.B.3: Создание Манифеста (Сборка JSON)**

  - **Действие:** Агент формирует финальный файл `slides_config.json`. Это "карта", по которой твой Python-скрипт соберет PPTX.
  - **Пример структуры JSON:**

```json
{ "presentation_title": "Базовые GET запросы",
  "slides": [
    {
      "layout_type": "single_wide",
      "title": "Качаем VS Code",
      "notes_file": "doc/lessons/lesson_03/presentations_03/slide_01.md",
      "images": [
        "images/Страница_загрузки_Visual_Studio_Code_с_вариантами_для_Windows_Linux_и_Mac.png"
      ]
    },
    {
      "layout_type": "single_tall",
      "title": "Обзор расширений VS Code",
      "notes_file": "doc/lessons/lesson_03/presentations_03/slide_02.md",
      "images": ["images/Список_расширений_VS_Code_для_Python.png"]
    },
    {
      "layout_type": "two_stack",
      "title": "Cline и Excalidraw",
      "notes_file": "doc/lessons/lesson_03/presentations_03/slide_03.md",
      "images": [
        "images/Карточка_расширения_Cline_в_VS_Code.png",
        "images/Карточка_расширения_Excalidraw_в_VS_Code.png"
      ]
    },
    {
      "layout_type": "two_tall_row",
      "title": "Спасибо за внимание!",
      "notes_file": "doc/lessons/lesson_03/presentations_03/slide_04.md",
      "images": [
        "images/Список_расширений_VS_Code_для_Python.png",
        "images/Список_расширений_VS_Code_для_Python.png"
      ]
    }
  ]
}
```

- **Подшаг 2.5.B.4: Сборка PPTX (Python Script)**

  - **Действие:** Запускается твой скрипт `JSON_to_PPTX`.
  - **Работа:**
        1. Читает `slides_config.json`.
        2. Для каждого слайда: открывает указанный `md` файл (читает ноуты), открывает указанный файл изображения.
        3. Вставляет всё в шаблон PPTX согласно `layout_mode`.
  - **Результат:** Готовый `lesson_03.pptx`.
  - В будущем предполагается что он сможет так же вставлять аудио дорожки с озвучкой к каждому слайду.

-----
