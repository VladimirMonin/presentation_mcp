# Руководство по миграции

Это руководство поможет вам мигрировать со старой версии системы (файлы `one.py`, `two.py`, `three.py`) на новую модульную архитектуру.

---

## Что изменилось

### Архитектура

**Было:**
- `one.py` — анализатор шаблонов
- `two.py` — очиститель текста
- `three.py` — генератор презентаций

**Стало:**
- Модульная структура с пакетами: `config/`, `core/`, `models/`, `io_handlers/`, `cli/`
- CLI интерфейс с командами `generate`, `analyze`, `help`
- Тесты с pytest
- Поддержка Markdown файлов для заметок
- Расширяемая система макетов

### Запуск команд

**Было:**
```bash
python three.py config.json
python one.py template.pptx
```

**Стало:**
```bash
python main.py generate --config config.json
python main.py analyze --template template.pptx
```

---

## Миграция JSON конфигураций

### notes_text → notes_source

**Было:**
```json
{
  "slides": [
    {
      "title": "Слайд 1",
      "notes_text": "Текст заметки",
      "images": ["photo.jpg"]
    }
  ]
}
```

**Стало:**
```json
{
  "slides": [
    {
      "title": "Слайд 1",
      "notes_source": "notes.md",  // теперь можно MD файлы!
      "images": ["photo.jpg"]
    }
  ]
}
```

**Важно:** Старые конфигурации с `notes_text` продолжают работать! Система автоматически мигрирует `notes_text` → `notes_source` при загрузке.

### Поддержка Markdown

Теперь можно использовать Markdown файлы для заметок:

```json
{
  "notes_source": "doc/notes/slide1.md"
}
```

Markdown автоматически конвертируется в plain text для PowerPoint.

### Новые возможности

1. **Контроль имени выходного файла:**
   ```json
   {
     "output_path": "presentation_final_v2.pptx"
   }
   ```

2. **Абсолютные пути:**
   ```json
   {
     "template_path": "C:/Templates/corporate.pptx",
     "notes_source": "C:/Docs/notes.md"
   }
   ```

3. **Смешивание абсолютных и относительных путей:**
   ```json
   {
     "template_path": "C:/Templates/template.pptx",
     "slides": [
       {
         "notes_source": "notes/slide1.md",  // относительно JSON
         "images": ["../shared/logo.png"]
       }
     ]
   }
   ```

---

## Пошаговая миграция

### Шаг 1: Обновление зависимостей

Убедитесь, что установлены все зависимости:

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `markdown` — для парсинга MD файлов
- `beautifulsoup4` — для очистки HTML
- `pytest` — для запуска тестов

### Шаг 2: Проверка старых конфигураций

Старые JSON файлы **должны продолжать работать** без изменений:

```bash
python main.py generate --config old_config.json
```

Если есть ошибки, проверьте:
- Пути к файлам (теперь относительные пути считаются от JSON)
- Имена макетов (должны существовать в шаблоне)

### Шаг 3: Миграция на Markdown (опционально)

Если хотите использовать Markdown для заметок:

1. Создайте MD файлы:
   ```markdown
   # Заметки к слайду 1
   
   - Пункт 1
   - Пункт 2
   ```

2. Обновите JSON:
   ```json
   {
     "notes_source": "notes/slide1.md"
   }
   ```

3. Или оставьте inline текст:
   ```json
   {
     "notes_source": "Обычный текст заметки"
   }
   ```

### Шаг 4: Обновление скриптов

Если у вас есть bash/shell скрипты:

**Было:**
```bash
#!/bin/bash
python three.py config1.json
python three.py config2.json
```

**Стало:**
```bash
#!/bin/bash
python main.py generate --config config1.json
python main.py generate --config config2.json
```

### Шаг 5: Архивирование старых файлов (опционально)

После успешной миграции:

```bash
mkdir archive
mv one.py two.py three.py archive/
```

---

## Примеры миграции

### Пример 1: Простая миграция

**Старый config.json:**
```json
{
  "template_path": "template.pptx",
  "layout_name": "Blank",
  "slides": [
    {
      "title": "Слайд 1",
      "slide_number": "1",
      "notes_text": "Заметка слайда 1",
      "images": ["photo1.jpg"]
    }
  ]
}
```

**Новый config.json (backward compatible):**
```json
{
  "template_path": "template.pptx",
  "layout_name": "Blank",
  "output_path": "output.pptx",
  "slides": [
    {
      "title": "Слайд 1",
      "slide_number": "1",
      "notes_source": "Заметка слайда 1",  // или "notes/slide1.md"
      "images": ["photo1.jpg"]
    }
  ]
}
```

**Запуск:**
```bash
# Старый способ
python three.py config.json

# Новый способ
python main.py generate --config config.json
```

### Пример 2: Миграция с Markdown

**Шаг 1:** Создайте MD файлы

`notes/slide1.md`:
```markdown
# Основные моменты

- Важный пункт 1
- Важный пункт 2

**Итог:** это будет автоматически очищено от форматирования
```

**Шаг 2:** Обновите JSON

```json
{
  "slides": [
    {
      "title": "Слайд 1",
      "notes_source": "notes/slide1.md",
      "images": ["photo1.jpg"]
    }
  ]
}
```

**Шаг 3:** Запустите

```bash
python main.py generate --config config.json --verbose
```

---

## Проверка результатов

### 1. Проверка генерации

```bash
python main.py generate --config your_config.json --verbose
```

Если видите ошибки, проверьте:
- Правильность путей
- Наличие всех файлов (изображения, MD файлы)
- Имена макетов

### 2. Сравнение результатов

Сгенерируйте презентацию старым и новым способом, сравните:
- Содержимое слайдов (заголовок, номер)
- Изображения (расположение, размеры)
- Заметки (текст)

### 3. Анализ шаблона

Если возникают проблемы с макетами:

```bash
python main.py analyze --template template.pptx
```

Это покажет все доступные макеты и их структуру.

---

## Troubleshooting

### Ошибка: "Layout not found"

**Причина:** Макет не найден в шаблоне.

**Решение:**
```bash
python main.py analyze --template template.pptx
```

Проверьте точное имя макета (case-sensitive).

### Ошибка: "notes_text not found"

**Причина:** Вы используете старую версию JSON.

**Решение:**
- Ничего не делайте! Система автоматически мигрирует `notes_text` → `notes_source`
- Или обновите вручную: `"notes_text"` → `"notes_source"`

### Ошибка: "Path not found"

**Причина:** Неправильные пути к файлам.

**Решение:**
- Проверьте, что относительные пути указаны относительно JSON файла
- Используйте `--verbose` для диагностики:
  ```bash
  python main.py generate --config config.json --verbose
  ```

### Markdown не работает

**Причина:** Отсутствует зависимость.

**Решение:**
```bash
pip install markdown beautifulsoup4
```

---

## Новые возможности

### 1. CLI команды

```bash
# Генерация с переопределением выходного файла
python main.py generate --config config.json --output result_v2.pptx

# Анализ конкретного макета
python main.py analyze --template template.pptx --layout "Blank"

# Справка
python main.py help
```

### 2. Verbose режим

```bash
python main.py generate --config config.json --verbose
```

Показывает:
- Загруженные конфигурации
- Процесс создания каждого слайда
- Размещение изображений
- Возможные предупреждения

### 3. Расширяемость

Теперь можно легко добавлять новые макеты — см. `doc/USAGE.md`, раздел "Расширяемость".

### 4. Тесты

Запуск тестов для проверки работоспособности:

```bash
pytest tests/
```

---

## Дополнительные ресурсы

- **Полное руководство:** `doc/USAGE.md`
- **План рефакторинга:** `doc/plan/refactor_plan.md`
- **Примеры конфигураций:** `doc/samples/`
- **Документация тестов:** `tests/README.md`

---

## Контрольный чеклист миграции

- [ ] Установлены все зависимости (`pip install -r requirements.txt`)
- [ ] Старые конфигурации работают с новой системой
- [ ] Обновлены bash/shell скрипты (если есть)
- [ ] Проверены пути к файлам (абсолютные/относительные)
- [ ] Протестирована генерация с `--verbose`
- [ ] Сравнены результаты со старой версией
- [ ] (Опционально) Мигрировано на Markdown для заметок
- [ ] (Опционально) Архивированы старые файлы
- [ ] Запущены тесты (`pytest tests/`)

---

Готово! Теперь вы используете новую модульную систему. 🎉
